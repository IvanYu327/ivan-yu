import React from "react";
import styled from "styled-components";

const CardContainer = styled.div`
  background: green;
`;

const ExperienceCard = () => {
  return <CardContainer>ExperienceCard</CardContainer>;
};

export default ExperienceCard;
